설명

www.makesense.ai 로 들어가기
Get Started -> 과제 제공 데이터셋의 Road(1) 부터 Road(150)까지 업로드
Object Detection 선택
아무것도 추가 안하고 Start project 선택
Actions -> import Annotations 선택
Multiple files in YOLO format ~ 선택
labels_my-project-name~ 폴더 업로드 -> import

Actions -> Edit Labels 에서 라벨 이름 옆의 새로고침 누르면 색깔이 바뀜
 -> 색깔 구분 잘되게 하면 편함
 
왼쪽 이미지들에서 체크표시 안된 이미지들에서 차량을 드래그해서 구분하고
오른쪽에 select label이 나오면 해당 차종으로 구분시키기

내보낼 때는
Actions -> Export Annotations 에서
A .zip package containing files in YOLO format -> Export
로 내보내기
